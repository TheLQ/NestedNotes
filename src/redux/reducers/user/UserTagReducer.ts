import { AnyAction } from "redux";

import { UserTagMap } from "../../../state/user/UserState";

export function UserTagReducer(state: UserTagMap, rawAction: AnyAction): UserTagMap {
	return state;
}
